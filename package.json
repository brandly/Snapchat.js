{
  "name": "snapchat.js",
  "description": "draw like snapchat",
  "author": "Matthew Brandly",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "open src/index.html",
    "deploy": "mkdir -p dist && cp src/* dist/ && uglifyjs src/snapchat.js > dist/snapchat.min.js && gh-pages --dist dist/"
  },
  "devDependencies": {
    "gh-pages": "3.0.0",
    "uglify-js": "3.9.4"
  }
}
